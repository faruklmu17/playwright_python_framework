# pytest.ini
[pytest]
markers =
    sample: quick setup verification test
    smoke: quick, high-value login/session tests
    bootstrap: run once to create storage_state.json

addopts = -m "not bootstrap"
