# .env.example

# Optional: set your base app URL if using a real app (not required for the demo page)
BASE_URL=

# These will be written by scripts/bootstrap_signup.py at first run
SIGNUP_NAME=
SIGNUP_EMAIL=
SIGNUP_PASSWORD=

# Where the authenticated session is stored/read
STORAGE_STATE=auth/storage_state.json
